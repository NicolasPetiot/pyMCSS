[project]
name = "pymcss"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NicolasPetiot", email = "nicolaspetiot2710@hotmail.fr" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
    "pandas>=2.3.2",
]

[build-system]
requires = ["uv_build>=0.8.9,<0.9.0"]
build-backend = "uv_build"
